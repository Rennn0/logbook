export enum SessionWindowEnum {
    startSession = 0,
    finishSession = 1,
    cancelSession = 2,
    lockBoatSession = 3,
    unlockBoatSession = 4,
    editStartedBoatSession = 5,
    forgottenBoatSession = 6,
    externalBoatSession = 7,
}

export enum dropDowns {
    obmannDropDown = 0,
    routeDropDown = 1,
    locationDropDown = 3,
    categoryTypeDropDown = 4,
    categoryDropDown = 5,
}

export enum formElementsIndex {
    externalSessionObmanInputIndex = 100,
    externalSessionCoxInputIndex = 200,
}